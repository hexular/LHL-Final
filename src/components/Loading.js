import React from 'react';
import Typography from '@material-ui/core/Typography';

const Loading = () => {
  return (
    <Typography>
      Loading Jobs...
    </Typography>
  );
}

export default Loading;